<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorPrimaryWarning"
    android:paddingStart="16dp"
    android:paddingLeft="16dp"
    android:paddingEnd="16dp"
    android:paddingRight="16dp"
    android:paddingBottom="32dp">

    <ImageView
        android:id="@+id/materialErrorProgressBar"
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:layout_gravity="center"
        android:layout_marginTop="32dp"
        android:background="@drawable/ic_dialog_warning"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/materialErrorDialogTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="-"
        android:textColor="@color/colorWhite"
        android:textSize="24sp"
        app:layout_constraintEnd_toEndOf="@+id/materialErrorProgressBar"
        app:layout_constraintStart_toStartOf="@+id/materialErrorProgressBar"
        app:layout_constraintTop_toBottomOf="@+id/materialErrorProgressBar" />

    <TextView
        android:id="@+id/materialErrorDialogDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:text="-"
        android:textAlignment="center"
        android:textColor="@color/colorLightWhite"
        app:layout_constraintEnd_toEndOf="@+id/materialErrorDialogTitle"
        app:layout_constraintStart_toStartOf="@+id/materialErrorDialogTitle"
        app:layout_constraintTop_toBottomOf="@+id/materialErrorDialogTitle" />

</androidx.constraintlayout.widget.ConstraintLayout>